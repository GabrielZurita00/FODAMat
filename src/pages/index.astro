---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
---

<Layout title="FODAMat">
	<main>
		<h1><span class="text-gradient">FODA</span>Mat</h1>
		<p class="instructions">
			Herramienta para la realización del <!--code>src/pages</code--> <strong>FODA Matemático</strong><br>
			Gestiona las áreas y administra las fortalezas, oportunidades, debilidades y amenazas de cada área
		</p>
		<ul role="list" class="link-card-grid">
			<Card
				href="/ejemplo/"
				title="Ejemplo"
				body="Enrutamiento de ejemplo para el proyecto"
			/>
			<Card
				href="/counter/"
				title="Contador"
				body="Contador con React y Vue"
			/>
			<Card
				href="/crear/"
				title="Continuar FODA"
				body="¿Ya tenías un FODA en realización? Continua el trabajo donde lo dejaste"
			/>
			<Card
				href="/nuevo/"
				title="Nuevo FODA"
				body="¿Deseas un nuevo comienzo? Crea un FODA"
			/>
		</ul>
	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1.5rem;
		max-width: 60ch;
	}
	h1 {
		font-size: 3rem;
		font-weight: 800;
		margin: 0;
		color: rgb(var(--blue-accent));
	}
	.text-gradient {
		background-image: linear-gradient(45deg, rgb(var(--red-accent)), rgb(255, 0, 0) 30%, rgb(255, 82, 83) 60%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 500%;
		background-position: 0%;
	}
	.instructions {
		line-height: 1.6;
		margin: 1rem 0;
		border: 1px solid rgba(var(--blue-accent), 25%);
		background-color: white;
		padding: 1rem;
		border-radius: 0.4rem;
	}
	.instructions code {
		font-size: 0.875em;
		font-weight: bold;
		background: rgba(var(--blue-accent), 12%);
		color: rgb(var(--blue-accent));
		border-radius: 4px;
		padding: 0.3em 0.45em;
	}
	.instructions strong {
		color: rgb(var(--red-accent));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
		gap: 1rem;
		padding: 0;
	}
</style>
